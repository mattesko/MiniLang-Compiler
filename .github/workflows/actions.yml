name: CI

on: [pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run valid and invalid program scanner and parser tests
      run: apt-get install bison -y && ./build.sh && ./test.sh
